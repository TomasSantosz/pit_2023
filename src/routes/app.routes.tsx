import React from "react";

import { createBottomTabNavigator } from '@react-navigation/bottom-tabs'

//const {Navigator, Screen } = createBottomNavigator();

export function AppRoutes(){
    return(
        <></>
    )
}